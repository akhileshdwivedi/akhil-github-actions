name: 'Secret Scanning Review'
on:
  workflow_dispatch:
    inputs:
      deploy_to_live:
        description: 'Deploy to live'
        type: boolean
        default: false
        required: true
  push:
    branches:
      - main

jobs:
  secret-scanning-review:
    runs-on: ubuntu-latest
    steps:
      - name: 'step1'
        run: |
          echo 'Hi'
      - name: 'step2'
        run: |
          echo 'Hey'
